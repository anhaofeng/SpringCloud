server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/anhaofeng/cloud-config.git
          search-paths:  #路径
            - foo
            - bar
